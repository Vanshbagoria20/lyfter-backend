version: "3.9"
services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:////data/app.db
      WEBHOOK_SECRET: testsecret
    volumes:
      - data:/data
volumes:
  data:
